services:
  - type: web
    name: kumbh-smart-seva
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: DATABASE_PATH
        value: database/main.db
